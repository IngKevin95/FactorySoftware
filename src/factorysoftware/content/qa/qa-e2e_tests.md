---
id: qa
steps:
  - id: e2e_tests
---

# e2e_tests

Sigue la secuencia de HU declarada en cada `FLUJO-N.md` correspondiente para probar las funcionalidades de principio a fin, usando navegación real de pantallas (si aplica).
Se instancia por cada `TEST-N` de tipo `e2e` del `plan.md` (una por flujo sin cobertura).
Garantiza el funcionamiento end-to-end real respetando el flujo verificado en la Arquitectura.
